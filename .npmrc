# This fork's Guardian experiment targets packages/dsh-skill-explorer only.
# Avoid rebuilding unrelated native/tunnel packages from the full monorepo on
# GitHub's Node 24 runner; the target package still runs its real tests/build.
ignore-scripts=true
